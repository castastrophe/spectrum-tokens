{
  "title": "Detail",
  "description": "Detail is a typography component used for disclosing extra information or smaller items in hierarchical relationships of text.",
  "x-category": "typography",
  "x-spectrum-website-url": "https://spectrum.adobe.com/page/detail/",
  "type": "object",
  "properties": {
    "script": { "$ref": "../shared/typography-script.json" },
    "weight": {
      "type": "string",
      "enum": ["light", "default"],
      "default": "default"
    },
    "size": { "type": "string", "enum": ["s", "m", "l", "xl"], "default": "m" }
  }
}
