{
  "title": "Heading",
  "description": "Heading is a typography component used to create various levels of hierarchies between text.",
  "x-tags": ["typography"],
  "x-spectrum-website-url": "https://spectrum.adobe.com/page/heading/",
  "type": "object",
  "properties": {
    "script": { "$ref": "../shared/typography-script.json" },
    "classification": { "$ref": "../shared/typography-classification.json" },
    "weight": {
      "type": "string",
      "enum": ["light", "default", "heavy"],
      "default": "default"
    },
    "size": {
      "type": "string",
      "enum": ["xs", "s", "m", "l", "xl", "xxl", "xxxl"],
      "default": "m"
    }
  }
}
