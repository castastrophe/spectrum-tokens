{
  "title": "Button group",
  "description": "A button group is a grouping of buttons whose actions are related to each other.",
  "x-category": "actions",
  "x-spectrum-website-url": "https://spectrum.adobe.com/page/button-group/",
  "type": "object",
  "properties": {
    "orientation": {
      "type": "string",
      "default": "horizontal",
      "enum": ["horizontal", "vertical"]
    },
    "size": { "type": "string", "enum": ["s", "m", "l", "xl"], "default": "m" },
    "isDisabled": { "type": "boolean", "default": false }
  }
}
